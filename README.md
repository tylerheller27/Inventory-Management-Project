# Inventory-Management-Project
#work in progress, puting notes in readme while i code

using Sannner .next() method for input validation to only grab the stuff before whitespace. this is kinda a gimmicky why of verifying only one thing is entered
